python-dotenv>=1.0.0
ddgs>=9.11.4
requests>=2.31.0
python-whois>=0.9.4
dnspython>=2.6.1
rich>=13.7.0
click>=8.1.7
aiohttp>=3.9.0
phonenumbers>=8.13.0
ipwhois>=1.2.0
bs4>=0.0.2
lxml>=5.1.0
certifi>=2024.0.0
Pillow>=10.0.0
exifread>=3.0.0
geopy>=2.4.0

# ── Optional: enable richer recon features ────────────────────────────────────
# Uncomment to install. These are detected at runtime with graceful fallback.
# instaloader>=4.13          # Full Instagram recon
# holehe>=1.20               # Email → 120+ site registration check
# maigret>=0.4.4             # Username OSINT across 3000+ sites
# theHarvester>=4.4.0        # Email/subdomain/IP gathering from public sources
dnstwist                     # Domain typosquatting detection (DomainTwistModule)
# compoundfiles              # Email forensics: MSG file parsing (EmailForensicsModule)
# outlook-msg                # Email forensics: Outlook MSG reader
# extract-msg                # Email forensics: MSG metadata extraction
# python-dateutil            # Email forensics: date parsing

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
ruff>=0.4.0
bandit>=1.7.0

# ── API Server (optional) ─────────────────────────────────────────────────────
# pip install fastapi uvicorn[standard] pydantic
# fastapi>=0.111.0
# uvicorn[standard]>=0.30.0
# pydantic>=2.7.0

# ── AI Summarization (optional) ───────────────────────────────────────────────
# pip install anthropic
# anthropic>=0.28.0
